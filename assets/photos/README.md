# Marker photos

Put map-location screenshots here. Use this path in the local editor's **Photo links** field:

`/assets/photos/your-image.jpg`

The Cloudflare deploy sync copies this folder into the public upload bundle automatically.
